interface:
  display_name: "PowerShell"
  short_description: "Diagnose PowerShell, native, and WSL boundaries"
  default_prompt: "Use $use-powershell-safely to diagnose this material Windows shell boundary, preserve process boundaries, and probe runtime readiness only when version behavior matters."
