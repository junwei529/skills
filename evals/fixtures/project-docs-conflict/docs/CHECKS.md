# Checks

The intended focused check is:

```text
python -m unittest discover -s tests -v
```

The latest result is unknown.

## Results Index

- Current evidence owner: `UNKNOWN`
- Historical partial check: the initial normalization test passed on
  2026-07-01; this time-bound result did not cover priority parsing

The historical entry is a frozen observation. Do not edit or relabel it as
current Feature A acceptance; a later current result must have its own owner.
