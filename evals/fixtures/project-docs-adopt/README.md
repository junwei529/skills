# Slug Seed

Slug Seed normalizes short labels into URL-safe lowercase slugs.

The library is intentionally small. It does not provide transliteration,
uniqueness checks, or a command-line interface.
