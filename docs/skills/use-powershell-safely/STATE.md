# PowerShell State

Last updated: 2026-07-31

## Current Implementation

Canonical editable SOURCE is the five-file package under
[`skills/use-powershell-safely`](../../../skills/use-powershell-safely) with
one entry and three conditional references.

Local `main` contains the reviewed boundary-hardening revision. It adds
executable-discovery cardinality and identity, one-base path resolution,
cross-shell newline contracts, task-specific sandbox remediation, and a
strictly bounded confirmed-Junction fallback. It keeps frontmatter and the
three-reference structure unchanged.

Public `main` and the installed `v0.1.1` package predate this revision and must
not be described as containing it.

## Current Writer

The SOURCE-behavior gate controller relinquished after evidence
synchronization. The commit-gate controller is authorized only through the
bounded current-state amend and relinquishes at local closeout; no later push,
candidate, install, or release authority is implied.

## Evidence State

The released `v0.1.1` revision has exact-source, DEV_DISCOVERY, RC, and stable
loaded-copy evidence. The later local hardening passes fifteen
deterministic assertions under PowerShell 7 and Windows PowerShell 5.1 and all
repository checks recorded at its run. Three fresh SOURCE-assisted behavior
contexts plus one same-context follow-up passed the current hardening matrix,
and an independent fresh assessor returned `ACCEPTED` with all eight Must
surfaces passing. It still lacks native selection, current loaded-copy
identity, RC/stable installation, live Bash/WSL, actual sandbox-denial, and
bounded shadow-use evidence. See [Verification](VERIFICATION.md).

## Next Gate

The next possible repository gate is a separately authorized push of local
`main`. RC installation and loaded-copy proof remain later independent gates.
Stable update, private-global retirement, retained-evidence cleanup, and
release remain later gates.

## Recovery Entry

1. Read [`AGENTS.md`](../../../AGENTS.md), the repository
   [Authority Map](../../AUTHORITY.md), and [Decision 0014](../../decisions/0014-powershell-portable-guidance-and-private-host-delta.md).
2. Read [Design](DESIGN.md), this state, and
   [Verification](VERIFICATION.md).
3. Inspect local/public Git identities and any later dirty scope against
   [Repository Handoff](../../HANDOFF.md).
4. Recompute the five SOURCE hashes before attributing any behavior.
5. Do not confuse the installed `v0.1.1` copy with this local SOURCE revision.
