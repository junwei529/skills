# Current Status

Last updated: 2026-07-31

## Current Stage

The complete first public baseline passed its cumulative native-review gate and
was committed locally as root commit
`624553b03c794bb8cbd24e6c454d251ac214e24f` on `main`. The working tree was
clean after that commit, and no remote was configured.

The user subsequently changed the current public GitHub account identifier
from `matsuri1987` to `junwei529`. Decision 0013 now owns the current
`junwei529/skills` repository locator and MIT public-attribution
identifier. Git resolves that governance-only correction at local commit
`24714788a4c489620643a58f5651a6a3888a8fd3`. At that point, a read-only
GitHub check found the new repository was not publicly reachable.

The user then authorized a source-only PowerShell global-rule migration
revision. Decision 0014 now separates documented portable guidance, bounded
inference from observed failures, and private host delta. The resulting
PowerShell and Project Docs SOURCE, eval, decision, and canonical-documentation
changes passed their local commit gate and were committed as
`c5d8e185ea9a9f32a4e98ac8ac364c4ebe915535`. No private global file,
installation, user configuration, tag, or release was changed by that work;
the separately authorized development discovery mappings remain outside
repository source.

At a later separately authorized publication gate, the public
[`junwei529/skills`](https://github.com/junwei529/skills) repository was
created without GitHub-generated initialization and received public `main` at
`c5d8e185ea9a9f32a4e98ac8ac364c4ebe915535`. The public branch ref and
GitHub's commit API resolved to that same SHA. This establishes public commit
visibility, not `RC_INSTALL`, stable-install, tag, or release evidence.

The exact PowerShell release candidate was later selected as clean public
commit `b0099c9d5ad986be5e893ceea13f1fc8b5aea384`. During its separately
authorized candidate gate, public `main` resolved to that SHA and
`skills/use-powershell-safely` was downloaded by exact commit into an isolated
`RC_INSTALL`. An initial test-home isolation attempt was rejected because a
development discovery entry remained visible; it is not counted as candidate
evidence.

The corrected repository-scoped canary exposed only the materialized
five-file candidate. All candidate Git blobs and raw SHA-256 values matched
the selected commit; the directory was not a reparse point; strict UTF-8,
no-BOM, fixture byte identity, and the single-same-name discovery boundary
passed. A native positive canary implicitly loaded the candidate entry plus
only the native/process and text/encoding references and correctly diagnosed
the parser/argument boundary. An ordinary `Get-Date` canary did not load the
Skill. Controller executions reproduced direct exit `0`, broken-wrapper exit
`1`, and deliberately split-argument exit `2` with the expected separated
streams.

This closed the PowerShell exact-commit candidate installation and loaded-copy
smoke at that gate. It did not establish a stable tag, stable installation,
release, Project Docs or Work Charter candidate, private-global shadow
migration, or cleanup. Exact sanitized evidence is owned by
[`docs/VERIFICATION.md`](VERIFICATION.md).

Nine isolated PowerShell contexts subsequently exercised the exact
development SOURCE or its catalog description. Seven boundary-sensitive
scenarios passed and the ordinary-cmdlet and POSIX-only canaries both declined
selection without loading the Skill body. No SOURCE correction was required.
Controlled native DEV_DISCOVERY canaries then proved implicit selection of the
canonical SOURCE for a material PowerShell boundary and non-loading for an
ordinary cmdlet. This closes development selection and loaded-copy identity,
but not candidate, stable-install, or release identity.

Work Charter M2R Tranche 1 was accepted after correction round 2. Tranche 2
then ran the approved SOURCE-assisted matrix in unique ignored fixture copies.
All five behavior groups and both controlled negative variants conformed to
their public contracts, including separate Planner/Executor and Standard O/P/E
topologies with one bounded correction each.

The sanitized report records the exact normalized canonical SOURCE identities,
role models and reasoning settings, observable actions, before/after
manifests, focused tests, cleanup, and limitations. The evidence writer has
completed its allowed writes and relinquished. The independent Planner
recomputed the five normalized SOURCE identities, reran the required checks,
and accepted M2R SOURCE-assisted development behavior for those exact hashes.

This evidence does not establish native discovery, implicit selection,
trigger telemetry, installed loaded-copy identity, candidate identity, or
release identity. Exact evidence is owned by
[`docs/VERIFICATION.md`](VERIFICATION.md).

Two later controlled native DEV_DISCOVERY canaries explicitly selected Work
Charter through the sole same-named repository-scoped discovery entry. Each
role's selection record identified the same canonical five-file SOURCE, and a
controller independently resolved the discovery target and rechecked the
unchanged hashes. The P/E canary completed with one read-only Planner, one
sole-writer Executor, five passing tests, `ACCEPTED`, and no correction. The
Standard canary completed the O-to-P-to-E route with a dormant read-only
Orchestrator, independent Planner acceptance, four passing tests, no
correction, and a stop before unapproved Phase Two.

Both controller audits confirmed the complete fixture boundary, exactly four
expected changed files, unchanged repository source and Git state, and cleanup
of task-specific discovery and fixture copies. This closes controlled native
explicit selection, DEV_DISCOVERY loaded-copy identity, and positive P/E and
Standard role delivery for the recorded Work Charter hashes. It does not close
implicit trigger telemetry, general Harness routing, real-project behavior,
`RC_INSTALL`, stable installation, or release identity.

The separately authorized Work Charter candidate gate then selected exact
public commit `78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09` and installed
`skills/work-charter` by exact SHA into an ignored isolated `RC_INSTALL`.
All five installed files matched the commit's Git blobs and recorded raw
SHA-256 values, decoded as strict UTF-8 without BOM, and formed the only
same-named Skill in the counted native scope.

An uncontaminated fresh canary implicitly selected that candidate from
continuity and recovery symptoms, read only the entry and recovery reference,
proposed P/E cold recovery, and stopped before role delivery or writes. A flat
arithmetic canary returned `703` without loading Work Charter. This closes the
Work Charter exact-commit candidate installation, representative implicit
selection, loaded-copy, and flat-task non-loading smoke. It does not establish
a stable tag, broad trigger telemetry, Standard behavior at the candidate
layer, real-project shadow use, or release identity.

Project Docs was aligned with the current official Codex Skill, AGENTS, and
approval documentation. The current six-file SOURCE now distinguishes explicit
user invocation from a target-project routing mention, follows project
instructions already loaded by the Harness, removes repeated native and
internal rules, and persists the explicit-reselection boundary in the
continuity anchor.

Five fresh explicit behavior groups and one persistence-only broken-routing
variant conformed at the new hashes. Controlled native DEV_DISCOVERY canaries
also proved explicit selection of canonical SOURCE and non-loading of the full
Skill body without explicit invocation. This is development loaded-copy
evidence; the later candidate result is recorded separately below.

The separately authorized shared-candidate gate then selected exact public
commit `78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09` for all three public
Skills. One exact-SHA `$skill-installer` request materialized Project Docs and
PowerShell into the same ignored isolated discovery scope; the retained Work
Charter candidate already used that commit. Controller audits proved the
expected 6/5/5 candidate files, SOURCE and commit equality, strict UTF-8 without
BOM, ordinary non-reparse directories, an exact 27-file shared scope, fixture
provenance, and exclusion of preflight artifacts from tracked source and
candidate evidence.

Counted native canaries used process-local candidate-only exclusions; no
persistent user configuration changed. PowerShell passed implicit native
boundary selection, loaded only its entry and two relevant references, and
stayed unloaded for `Get-Date`; controller reproduction returned the expected
separate-stream exits `0`, `1`, and `2`. Project Docs passed an explicit
standalone mature-project `NOOP`, loaded only its entry and audit reference, and
stayed unloaded for an ordinary documentation-routing question; its focused
test passed. Preflights that failed candidate-only discovery, complete prompt
transport, standalone Skill isolation, or semantic completion are excluded
from behavior and cost claims.

This closes one shared exact-commit `RC_INSTALL` identity and representative
positive/negative loaded-copy smoke for all three Skills. It does not establish
a stable tag, `STABLE_INSTALL`, broad trigger telemetry, controlled token cost,
real-project shadow behavior, private-global retirement, or release identity.

The separately authorized tag and stable-install gates then published
immutable tag
`v0.1.0` at the same commit-smoked candidate
`78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09` and installed all three
Skills from that public tag through the bundled `$skill-installer`. Before
changing discovery state, the complete set was materialized in isolation and
proved to contain exactly 6/5/5 files matching the tag's Git blobs, with strict
UTF-8, no BOM, no reparse points, and three passing Skill validators.

The authorized recoverable switch moved one Project Docs development junction
and the previous byte-equivalent PowerShell directory into a non-discoverable
local rollback area without deleting them. The resulting user-level Codex
installation exposes exactly one known discoverable copy of each Skill.
Three read-only ephemeral Codex processes then loaded those installed entries,
recomputed their entry hashes, and returned the expected bounded behavior:
Project Docs `NOOP`, Work Charter `Flat`, and the PowerShell requirement to
capture `$LASTEXITCODE` immediately. This closes tag-pinned stable-install
identity and explicit loaded-copy smoke. It does not establish stable mutation
behavior, broad implicit trigger telemetry, real-project adoption, controlled
cost, cross-Harness behavior, private-global retirement, or a GitHub Release
object.

Native review of the evidence sync found that the immutable tag snapshot's own
README predates publication and still says `v0.1.0` is absent. The tag-to-install
proof remains valid, but the contradictory tagged documentation means
`v0.1.0` is not the coherent public-release identity. The tag must remain
immutable as evidence; correction requires a new authorized patch-version
identity and repeated tag-pinned installed-copy proof.

## Completed

- The complete 139-file public baseline passed deterministic verification and
  the cumulative native-review gate, then became local root commit
  `624553b03c794bb8cbd24e6c454d251ac214e24f`.
- The three independent public Skills and lifecycle boundary are accepted;
  canonical decisions own rationale and
  [`docs/RUNBOOK.md`](RUNBOOK.md) owns operational procedure.
- Project Docs M1R current exact-hash SOURCE-assisted behavior and the earlier
  PowerShell scope are accepted as historical development evidence; exact
  evidence and limitations are owned by
  [`docs/VERIFICATION.md`](VERIFICATION.md).
- Work Charter Decision 0012 is implemented as the sole five-file
  `work-charter` SOURCE plus five cases and two shared fixtures.
- The Work Charter correction rounds 1 and 2 contract edits and documentation
  deduplication are complete; no M2R file was staged or committed.
- The Work Charter SOURCE-assisted matrix completed five behavior groups and
  two controlled negative variants; the ignored run root was removed after
  manifest and focused-test evidence capture.
- Independent Planner acceptance closed M2R SOURCE-assisted development
  behavior for the exact recorded hashes.
- Work Charter controlled native DEV_DISCOVERY P/E and Standard canaries passed
  explicit selection, same-SOURCE loaded-copy, role-delivery, fixture-boundary,
  focused-test, and cleanup checks without a correction round.
- The exact public Work Charter candidate commit
  `78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09` passed `$skill-installer`
  download, five-file commit identity, validator, uncontaminated implicit P/E
  recovery selection, loaded-copy, and flat-task non-loading checks.
- Project Docs official-manual alignment, five explicit behavior groups, one
  persistence-only broken-routing variant, and controlled native
  DEV_DISCOVERY positive/negative canaries passed for the current six hashes.
- Shared public candidate
  `78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09` passed exact-SHA
  `$skill-installer` materialization, 6/5/5 file identity, strict-encoding,
  non-reparse, shared-scope, standalone Project Docs, repeated PowerShell, and
  retained same-commit Work Charter loaded-copy checks.
- Public tag `v0.1.0` resolves to that accepted candidate, and one complete-set
  tag-pinned `STABLE_INSTALL` passed 6/5/5-file identity, strict encoding,
  no-reparse, single-discovery, validator, and explicit fresh-process
  loaded-copy smoke for Project Docs, Work Charter, and PowerShell.
- The PowerShell portable-guidance migration SOURCE passes its Skill validator,
  metadata assertion, repository and fixture checks, PowerShell parser check,
  whitespace check, and Gitleaks scan. Its normalized hashes and evidence
  boundary are recorded in the sanitized development result.
- The same exact PowerShell SOURCE passed seven boundary-sensitive
  fresh-context scenarios; two catalog-assisted negative canaries returned
  `NO_TRIGGER` without loading the Skill body.
- PowerShell controlled native DEV_DISCOVERY positive/negative canaries proved
  implicit selection of canonical SOURCE and non-loading for an ordinary
  cmdlet; that development discovery result remains historical and does not
  describe the current stable discovery state.
- The public `junwei529/skills` repository exists, and the first public `main`
  push was verified at
  `c5d8e185ea9a9f32a4e98ac8ac364c4ebe915535`.
- The exact public PowerShell candidate commit
  `b0099c9d5ad986be5e893ceea13f1fc8b5aea384` passed isolated
  `$skill-installer` download, five-file commit identity, native
  implicit-selection, loaded-copy, boundary diagnosis, and ordinary-cmdlet
  non-loading checks.
- The candidate Skill validator, repository checker, all fourteen fixture
  preconditions, whitespace check, Gitleaks scan, strict UTF-8/no-BOM checks,
  and controller native fixture executions passed.

Historical checks, findings, and behavior evidence remain only in
[`docs/VERIFICATION.md`](VERIFICATION.md).

## In Progress

- No Skill SOURCE implementation tranche is active. The authorized
  `v0.1.1` patch-release candidate changes public and governance documentation,
  including a complete Simplified Chinese README, while keeping all three
  Skill packages unchanged.
- No discovery or installation mutation is active. The stable installed copies
  are derived from `v0.1.0` and remain non-editable; the prior Project Docs
  junction and PowerShell directory remain retained locally for rollback.
- Historical and shared candidate evidence roots remain retained locally.
  Cleanup, update, rollback, GitHub Release creation, and private-global
  migration remain separately gated.

## Known Gaps

- Project Docs has exact-hash SOURCE-assisted and DEV_DISCOVERY evidence plus
  one shared-candidate explicit standalone `NOOP` and ordinary-routing
  non-loading pair. Its tag-pinned stable entry and explicit `NOOP` smoke now
  pass. Mutation behavior at the candidate or stable layer, broad trigger
  telemetry, and unrelated real-project adoption remain unproved.
- Work Charter has SOURCE-assisted synthetic evidence plus controlled native
  DEV_DISCOVERY explicit-selection, same-SOURCE loaded-copy, P/E, and Standard
  role-delivery evidence. Its exact-commit `RC_INSTALL` also passes one
  uncontaminated implicit P/E recovery canary and one flat non-loading canary.
  Its tag-pinned stable entry and explicit Flat smoke also pass. Broad trigger
  telemetry, Standard candidate or stable behavior, general Harness routing,
  and independent real-project behavior remain unproved.
- The controlled capability variants are not real-Harness outage evidence.
- The PowerShell portable-guidance migration has exact-SOURCE behavior evidence
  for seven boundary scenarios and catalog-assisted negative evidence for two
  exclusions. Controlled native DEV_DISCOVERY selection and loaded-copy
  identity also pass. Historical and shared exact-commit `RC_INSTALL`,
  candidate identity, native candidate selection, and loaded-copy smoke pass.
  Its tag-pinned stable entry and explicit `$LASTEXITCODE` smoke pass. It still
  lacks controlled-host runtime execution of every version branch, stable
  implicit-selection telemetry, and the shadow period.
- No efficacy, token comparison, independent real-project adoption, or
  cross-Harness compatibility claim is established.
- Project Docs, PowerShell, and Work Charter have actual loaded-copy proof at
  one shared exact-commit `RC_INSTALL` and one tag-pinned `STABLE_INSTALL`.
  The stable canaries were explicit, narrow behavior checks rather than broad
  trigger or real-project evidence.
- The immutable `v0.1.0` snapshot's README incorrectly describes that tag as
  absent. Its tag-to-install evidence passes, but a documentation-consistent
  public release identity has not yet been established.

## Open Questions

No Project Docs, Work Charter, or PowerShell v0.1 product-contract decision
remains open. Remaining exact-source, selection, loaded-copy, and lifecycle
evidence are workflow gates, not product decisions.

## Next Gate

Shared candidate alignment, immutable `v0.1.0` tag publication, and tag-pinned
stable-installation evidence are complete at
`78caaa6ecf6b86c1b7be26d03aa5dfde1ebbdf09`. Later evidence-only commits
do not become a release identity and must not move `v0.1.0`.

The user authorized a new immutable `v0.1.1` patch-version candidate containing
self-consistent English and Simplified Chinese release documentation. The
current gate is to review, commit, and push that candidate; materialize and
smoke the exact candidate commit; create and push immutable tag `v0.1.1`; then
repeat tag-pinned stable installation and loaded-copy proof. `v0.1.0` must not
move. No GitHub Release object, retained-evidence cleanup, private-global edit,
or broader release claim is authorized. Bounded real-project shadow use must
still precede retirement of duplicated private-global semantics.
